const initialRadiis = [10, 1/2, 1/3, 1/3, 1/3, 1/10, 1/3];
const initialSpeeds = [-Math.PI / Math.random(), Math.random() * Math.PI / Math.random(), Math.random() * -Math.PI / Math.random(), Math.random() * Math.PI / Math.random(), Math.random() * -Math.PI / Math.random(), Math.random() * Math.random() * Math.PI / Math.random()];
const initialDraws = [0, 0, 0, 0, 0, 1];

export {initialSpeeds, initialDraws, initialRadiis};
